name: PHP Validation

on: [push]

jobs:
  check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: PHP Lint
        run: find . -name "*.php" -exec php -l {} \;
